import SwissCodesClientPage from "./SwissCodesClientPage"

export const metadata = {
  robots: {
    index: false,
    follow: false,
  },
}

export default function SwissCodesPage() {
  return <SwissCodesClientPage />
}
